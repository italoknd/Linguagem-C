#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main()( int r, x = 5, y = 3; r = (x > 2) && (y < x);//verdadeiro (1) printf("Resultado: %d\n",r); r = (x%2==0) && (y > 0);//falso (0) printf("Resultado: %d\n",r); r = (x > 2) II (y > x);//verdadeiro (1) printf("Resultado: %d\n",r); r = (x%2==0) II (y < 0);//falso (0) printf("Resultado: qsd\n",r); r = !(x > 2);// falso (u) printf("Resultado: %d\n",r); r = !(x > 7) && (x > y);// verdadeiro (1) printf("Resultado: %d\n",r); 
return 0; 
} 
